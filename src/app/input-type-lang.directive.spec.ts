import { InputTypeLangDirective } from './input-type-lang.directive';

describe('InputTypeLangDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTypeLangDirective();
    expect(directive).toBeTruthy();
  });
});
